const config = {
    port: 3000,
    database: {
          DATABASE: 'nodesql'
        , USERNAME: 'nodeuser'
        , PASSWORD: '12345'
        , HOST: 'localhost'
        , PORT: '3306'
    }
}

module.exports = config
